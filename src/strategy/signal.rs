// use excenter::{prelude::*, excenter::{ExCoinPairSummary, ExGlobalSummary}, kunlun::models::KunlunStrategyInfo};
// use exlib::prelude::*;
// use tokio::sync::oneshot;

#[derive(Debug, PartialEq, Eq)]
pub enum EverestStrategySignal {
    #[allow(non_camel_case_types)]
    WILL_EXIT,
}
